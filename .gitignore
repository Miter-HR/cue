# Dependencies
node_modules/

# Build output
dist/
out/
.cache/

# Native binaries
native/systemaudio

# Logs
*.log

# macOS
.DS_Store

# User data / secrets — NEVER commit these
cue-data.json        # contains API keys + personal interview prep
*.env
.env*

# Test artefacts
test-stt-connection.js
test-resume.js

# Kiro IDE internals — hide spec/steering from public repo
.kiro/

# Electron dist cache
node_modules/electron/dist/electron.exe.bak

# Knowledge base — internal Miter/Slite content, never commit. Keep the
# sources config and README so a fresh clone knows how to rebuild it.
knowledge-base/*
!knowledge-base/README.md
!knowledge-base/slite-sources.json

# Real call exports used as local test data — customer names/emails, never commit
test_transcripts/
